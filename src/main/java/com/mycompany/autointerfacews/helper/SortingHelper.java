/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package com.mycompany.autointerfacews.helper;

import com.mycompany.autointerfacews.dataIcon.Action;
import com.mycompany.autointerfacews.mydata.MyTable;
import java.util.Collections;
import java.util.Comparator;
import java.util.List;

/**
 *
 * @author jupiter
 */
public class SortingHelper {

    // The comparators


    static public void sorting(MyTable table, Action action) {
//        int index = table.getColNames().indexOf(action.getAttr());
//        Comparator<List<String>> comparator_low_to_high = new Comparator<List<String>>() {
//            @Override
//            public int compare(List<String> o1, List<String> o2) {
//                return o1.get(index).compareTo(o2.get(index));
//            }
//        };
//        Comparator<List<String>> comparator_high_to_low = new Comparator<List<String>>() {
//            @Override
//            public int compare(List<String> o1, List<String> o2) {
//                return o2.get(index).compareTo(o1.get(index));
//            }
//        };
//        
//        if (action.getAct().equals("sort low to high"))
//            Collections.sort(table.getData(), comparator_low_to_high);
//        else if (action.getAct().equals("sort high to low")) {
//            Collections.sort(table.getData(), comparator_high_to_low);
//        }
    }

}
